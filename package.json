{
  "scripts": {
    "build": "npm run deploy_back && npm run build_front",
    "install": "npm run deploy_back && npm run install_front",
    "deploy_back":"cd ./backend && npm i && npm run deploy && cd ..",
    "build_front": "cd ./v-spoms && npm run build && cd ..",
    "install_front": "cd ./v-spoms && npm i && cd ..",
    "dev": "npm run install && cd ./v-spoms && npm run dev",
    "host": "cd ./v-spoms && npm run host && cd ..",
    "test_backend": "cd ./backend && jest"
  }
}
